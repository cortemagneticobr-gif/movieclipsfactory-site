Present so GitHub Pages serves this repository verbatim instead of running
Jekyll over it. Jekyll filters out dot-files and underscore-folders, which is
exactly what a platform ownership-verification file looks like.
